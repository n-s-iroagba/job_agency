1. use  .env files to store secrets and environment variables
2. separate concerns into different files
3. use a consistent naming convention
4. use a consistent coding style
5. use a consistent folder structure
6. use a consistent commit message format
7. use a consistent pull request format
8. use a consistent code review format
9. use zod for schema validationg
10. use sequelize for database operations
11. use express for routing
12. use bcrypt for password hashing
13. use jsonwebtoken for authenticatio
15. use nodemailer for email notifications
16. code should be modular and reusable
17. code should be well-documented
18. code should be well-tested
19. code should be well-documented
20. folder structure should be as follows:
    - controllers
    - repositories
    - middlewares (auth, rbac, errorHandler)
    - dtos
    - models
    - routes
    - services
    - utils
    - middleware
    - config
    - data
    - .env
    -app.ts
    -index.ts
    -gitignore
21. use typescript and express
21. first create models and seed database then proceed upwards to repos, utils, helpers, services, controllers, routes, middleware, config, data
22. No string literals, use constants from constants.ts
    


